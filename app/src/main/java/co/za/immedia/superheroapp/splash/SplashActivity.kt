package co.za.immedia.superheroapp.splash

import android.media.MediaPlayer
import android.os.Bundle
import android.os.Handler
import androidx.appcompat.app.AppCompatActivity
import co.za.immedia.superheroapp.R
import co.za.immedia.superheroapp.dashboard.DashboardActivity


class SplashActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        //setContentView(R.layout.activity_splash)

       // goToActivityWithNoPayload(DashboardActivity::class.java, FADE_IN_ACTIVITY)
    }
}